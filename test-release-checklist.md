# 发布前测试清单

## 使用方式
逐条手动测试，通过打 [x]，失败打 [ ] 并记录原因。

---

## 1. 主链路回归（8 条核心路径）

### 1.1 注册
- [ ] 访问 `/register`，输入用户名、邮箱、密码，提交
- [ ] 注册成功后自动登录，跳转到首页
- [ ] 使用相同邮箱再次注册 → 提示"该邮箱已被注册"
- [ ] 使用相同用户名再次注册 → 提示"该用户名已被占用"

### 1.2 登录
- [ ] 使用邮箱+密码登录成功
- [ ] 使用用户名+密码登录成功
- [ ] 错误的密码 → 提示"邮箱/用户名或密码错误"
- [ ] 登录后右上角显示用户名

### 1.3 上传简历
- [ ] 免费用户上传第一份简历 → 成功
- [ ] 免费用户上传第二份简历 → 拦截（免费版最多管理 1 份简历）
- [ ] Pro 用户上传多份简历 → 成功
- [ ] 上传 PDF/DOCX 格式 → 成功
- [ ] 上传其他格式（.exe）→ 拒绝

### 1.4 智能分析
- [ ] 选择简历 + 选择/输入目标岗位 → 开始分析
- [ ] 免费用户分析成功 → 消耗每日额度
- [ ] 免费用户额度耗尽 → 提示升级
- [ ] 分析完成后可查看结果

### 1.5 岗位推荐
- [ ] 选择简历后 → 加载推荐列表
- [ ] 点击感兴趣/不感兴趣 → 反馈记录
- [ ] 点击「加入看板」→ 投递记录出现在看板

### 1.6 模拟面试
- [ ] 选择简历 + JD + 面试风格 → 创建面试
- [ ] 进入面试房间 → 收到第一道题
- [ ] 提交回答 → 收到评分
- [ ] 面试完成后 → 可查看报告
- [ ] 免费用户每日 5 次限额 → 超额拦截

### 1.7 投递看板
- [ ] 从推荐页添加投递 → 出现在「待投递」列
- [ ] 拖拽卡片到「已投递」→ 状态更新
- [ ] 列表视图可正常切换
- [ ] 批量选择 → 批量操作

### 1.8 订阅升级
- [ ] 访问 `/subscription` → 看到三个套餐
- [ ] 点击「升级到 Pro」→ 弹出确认框
- [ ] 确认 → 创建订单 → mock-pay → 成功提示
- [ ] 支付成功后 → 个人中心显示 Pro 标签
- [ ] Pro 用户可使用 ATS 检测、Offer 决策等高级功能

---

## 2. 权限回归（5 条）

### 2.1 普通用户不可见内部页
- [ ] 访问 `/admin` → 被重定向到首页
- [ ] 访问 `/prompt-traces` → 被重定向到首页
- [ ] 侧边栏无「管理工具」菜单

### 2.2 Admin 用户可见
- [ ] 数据库直接设置 role='admin'
- [ ] 重新登录后侧边栏显示「管理工具」
- [ ] 可访问 `/admin/users` → 看到用户列表
- [ ] 可访问 `/admin/orders` → 看到订单列表

### 2.3 免费用户被拦截
- [ ] 上传第 2 份简历 → 拦截（ERR_QUOTA）
- [ ] 分析额度耗尽 → 拦截
- [ ] 面试额度耗尽 → 拦截
- [ ] 访问 ATS 检测 → 提示升级

### 2.4 Pro 用户不被误拦截
- [ ] Pro 用户上传多份简历 → 正常
- [ ] Pro 用户使用 ATS 检测 → 正常
- [ ] Pro 用户使用 Offer 决策 → 正常

### 2.5 接口权限
- [ ] 用户 A 无法访问用户 B 的简历
- [ ] 未登录访问受保护接口 → 401

---

## 3. 异常回归（6 条）

### 3.1 上传失败
- [ ] 上传超过 10MB 文件 → 拒绝
- [ ] 上传非 PDF/DOCX/图片格式 → 拒绝

### 3.2 模型超时
- [ ] 分析过程中模拟网络断开 → 提示超时
- [ ] 超时后重新提交 → 可正常重试

### 3.3 支付中断
- [ ] 创建订单后未支付 → 订单状态为 pending
- [ ] 重复调用 mock-pay 同一订单 → 幂等处理，不重复开通

### 3.4 额度耗尽
- [ ] 免费用户所有额度用完 → 各入口显示升级提示
- [ ] 跨天后额度自动重置 → 可继续使用

### 3.5 会话过期
- [ ] Token 过期后请求 → 401，跳转登录
- [ ] 重新登录后可继续使用

### 3.6 邮箱验证
- [ ] 注册后邮箱未验证 → 有提示
- [ ] 调用发送验证邮件 → 生成 token
- [ ] 调用验证接口 → 标记已验证

---

## 4. 生产部署演练

### 4.1 环境准备
- [ ] `.env` 文件已配置（数据库密码、JWT 密钥、LLM 配置）
- [ ] `ADMIN_USERNAMES` 已设置管理员账号

### 4.2 部署步骤
```bash
# 1. 构建镜像
docker compose -f docker-compose.prod.yml build

# 2. 运行数据库迁移
docker compose -f docker-compose.prod.yml --profile tools run --rm migrate

# 3. 启动服务
docker compose -f docker-compose.prod.yml up -d

# 4. 检查服务状态
docker compose -f docker-compose.prod.yml ps
```

- [ ] 步骤 1-4 全部执行成功

### 4.3 健康检查
- [ ] 访问 `http://host:8000/health` → 返回 `{"status": "ok"}`
- [ ] 访问前端 → 页面正常加载
- [ ] 可正常登录

### 4.4 数据持久化
- [ ] 重启容器后 MySQL 数据不丢失
- [ ] 重启容器后上传文件不丢失

### 4.5 回滚步骤
```bash
# 恢复数据库（回滚到上一个版本）
docker compose -f docker-compose.prod.yml --profile tools run --rm migrate-downgrade

# 恢复代码版本
git checkout [上一个稳定版本标签]

# 重新部署
docker compose -f docker-compose.prod.yml up -d --build
```

---

## 5. 上线前 Checklist

```
□ 主链路 8 条全部通过
□ 权限回归 5 条全部通过
□ 异常回归 6 条全部通过
□ 生产部署演练完成
□ 回滚方案已确认
□ 环境变量清单已更新
□ 数据库迁移已验证
□ SSL 证书已配置
□ 数据库自动备份已配置
□ 关键接口已加入健康检查
```

---

## 6. Pilot 环境实施回归（T5-2，第二个客户上线前逐条打勾）

> 流程细节见 `docs/pilot-plan.md` §4；实施后把结果与耗时填入 `docs/pilot-report.md` §2。

### 6.1 建租户与品牌
- [ ] `POST /admin/tenants` 创建 Pilot 客户租户成功（name/slug/plan_tier/expires_at 生效）
- [ ] 前端「运营后台 → 租户管理」可见该租户，带到期倒计时 tag
- [ ] `PUT /admin/tenants/{id}/brand` 更新 logo/主色/company/contact 后，前端登录页刷新生效
- [ ] `POST /admin/tenants/{id}/domains` 绑定主域名（is_primary=1）成功

### 6.2 配套餐与题型
- [ ] `POST /subscription/admin/plans` 自定义 pro 套餐名/价格，订阅页展示自定义套餐
- [ ] `InterviewQuestionBank` 按租户覆盖题型模板/静态题生效（前端面试题来自租户题库）
- [ ] `InterviewScoringRule` 按租户配置评分维度/权重生效

### 6.3 导入岗位与知识
- [ ] `POST /admin/tenants/{id}/jobs` 批量导入岗位成功，岗位仅在该租户可见
- [ ] `POST /admin/tenants/{id}/knowledge` 上传客户文档成功，知识检索命中客户内容
- [ ] 种子知识导入后 RAG 检索有引用证据可回看

### 6.4 数据隔离
- [ ] 该租户数据在其他租户（对照租户）登录后不可见（简历/岗位/知识/报表）
- [ ] 到期或停用后该租户用户访问被拦截（403，code=-13）

### 6.5 客户管理员可用
- [ ] 客户管理员账号可登录，能查看本租户状态与报表（看不到其他租户）
- [ ] 客户管理员不可访问平台级运营后台（租户管理/全局报表）

### 6.6 全流程可用
- [ ] 该租户下注册 → 传简历 → 智能分析 → 模拟面试 → 岗位推荐 → 职业规划 全链路可跑通
- [ ] 额度体系生效（free/pro 限额差异可验证）
