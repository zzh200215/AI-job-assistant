"""行级简历改写 Prompt（B1.2）

只允许对给定的锚点块产出「原文 → 改后」，锚点之外不得发声。
"""

RESUME_REWRITE_PROMPT = """你是一名资深简历编辑，工作内容是逐条改写候选人已经写下的文字，让它更具体、更有结果导向。

【可改写块（锚点清单）】
{blocks_json}

每个块含 block_id（锚点）、label（所属栏目）、text（原文）。

【目标岗位（可选，用于决定改写侧重）】
{jd_text}

【硬性规则】
1. 每条建议的 block_id 必须逐字取自上面的清单；不在清单里的内容一律不改，也不得为它生成建议；
2. 不得新增候选人没有写过的事实：不能出现新的公司、职位、时间、技术栈、数字或百分比。允许做的是把已有事实写清楚——用原文里已有的数字、把被动描述改成"动作 + 对象 + 结果"、拆长句、去掉空话；
3. 原文里没有量化数据时，就不要造数字，宁可只把表达改清楚；
4. original 必须与所在块的 text 完全一致（逐字复制），否则这条建议无效；
5. 每个 block_id 最多一条建议；最多输出 {max_suggestions} 条，只挑真正值得改的；没有值得改的就返回空数组，不要凑数；
6. 改后文本保持中文、保持原有换行结构（多行仍用 \\n 分隔），长度不超过原文的 2 倍。

【输出要求】
只输出 JSON，不要任何解释文字：
{{
  "suggestions": [
    {{
      "block_id": "清单中的锚点",
      "original": "该块的 text 原文",
      "proposed_text": "改后文本",
      "reason": "一句话说明改了什么、依据原文哪部分"
    }}
  ]
}}
"""

__all__ = ["RESUME_REWRITE_PROMPT"]
